Ask user for a year.
Handle invalid or non-numeric input using try/except.
If year ≤ 0, raise ValueError with "Year must be positive"
If divisible by 400 → "Leap year"
Else if divisible by 100 → "Not a leap year"
Else if divisible by 4 → "Leap year"
Else → "Not a leap year"
