Ask the user to enter a username.

Check that:

It’s at least 5 characters long.

It contains only letters or digits (.isalnum()).

It starts with a letter (.isalpha()).

Print appropriate feedback messages.

Use try/except to handle empty input.
